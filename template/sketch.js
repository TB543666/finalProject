let siriWave = new SiriWave({
  width: 1910,
  height: 200,
  speed: 0.03,
  container: document.getElementById("app"),
  autostart: true,
});



function setup() {
  createCanvas(windowWidth, windowHeight);
  background(255);

}
